apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx-demo.fullname" . }}
data:
  index.html: |
    <h1>{{ .Values.content.message }}</h1>
    <p>Env: {{ .Values.envName | default "default" }}</p>